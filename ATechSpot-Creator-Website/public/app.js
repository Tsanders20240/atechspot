(()=>{'use strict';
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(!reduced&&'IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>io.observe(el))}else document.querySelectorAll('.reveal').forEach(el=>el.classList.add('in'));
document.querySelectorAll('form[data-api]').forEach(form=>{const started=form.querySelector('[name="form_started_at"]');if(started)started.value=String(Date.now());form.addEventListener('submit',async e=>{e.preventDefault();if(!form.reportValidity())return;const btn=form.querySelector('button[type="submit"]'),status=form.querySelector('.status'),original=btn?.textContent||'';if(btn){btn.disabled=true;btn.textContent='Sending securely…'}if(status){status.textContent='Sending your request securely…';status.removeAttribute('data-error')}const data=Object.fromEntries(new FormData(form).entries());try{const r=await fetch(form.dataset.api,{method:'POST',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify(data)});let j={};try{j=await r.json()}catch{}if(!r.ok)throw new Error(j.message||'We could not send the request.');if(status)status.textContent=j.message||'Thank you. Your creator request was received.';form.reset();if(started)started.value=String(Date.now())}catch(err){if(status){status.textContent=err.message||'We could not send this request right now. Please try again shortly.';status.dataset.error='true'}}finally{if(btn){btn.disabled=false;btn.textContent=original}}})});
})();