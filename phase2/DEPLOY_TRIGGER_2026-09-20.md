# Phase 2 deployment trigger

This commit exists only to force Cloudflare Pages to build the current `phase2/shared-platform-foundation` branch after the Phase 2 Functions, migrations, routing, and preview-app selector were added.
