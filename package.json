{
  "name": "atechspot-cloudflare-pages",
  "private": true,
  "type": "module",
  "version": "25.0.0",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name atechspot --branch main",
    "deploy:preview": "wrangler pages deploy . --project-name atechspot --branch preview"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
