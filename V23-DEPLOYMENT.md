# V23 Deployment

1. Upload the full V23 folder contents to a staging deployment first.
2. Verify `/`, `/intake`, `/contact`, `/booking`, `/payment`, `/data-recovery`, `/business-audit.html`, and `/ai-readiness.html`.
3. Test desktop, tablet, and mobile navigation.
4. Test the intro Enter Site button and Escape key.
5. Verify GA4, Microsoft Clarity, BotPenguin, Zoom, forms, and Cloudflare `/api/*` endpoints.
6. Confirm DriveSavers partner code DS25379 remains present where intended.
7. Only after staging validation, deploy to the production AtechSpot domain.
